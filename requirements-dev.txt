# Test/lint dependencies — install alongside requirements.txt
pytest>=8
respx>=0.21
ruff>=0.4
# Tier 3 (real PostgreSQL) — used locally when TEST_DATABASE_URL is unset.
# CI provides Postgres via a services: block instead.
testcontainers[postgres]>=4 ; python_version >= "3.9"
